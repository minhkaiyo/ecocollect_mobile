{
  "name": "ecocollect",
  "version": "1.0.0",
  "scripts": {
    "build": "if [ ! -d \"flutter-sdk\" ]; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable flutter-sdk; fi && export PATH=\"$PATH:$(pwd)/flutter-sdk/bin\" && flutter config --enable-web && flutter build web --release"
  }
}
